# B3nd Wallet Server Configuration

# Server port
WALLET_PORT=8843

# B3nd backend URLs
# Credential backend - stores user keys, passwords, and reset tokens
CREDENTIAL_NODE_URL=http://localhost:8080

# Proxy backend - where user writes are forwarded to
# Can be the same as CREDENTIAL_NODE_URL in development
PROXY_NODE_URL=http://localhost:8080

# JWT Configuration
# Generate with: head -c 32 /dev/urandom | base64
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# JWT token expiration (in seconds, default 86400 = 24 hours)
JWT_EXPIRATION_SECONDS=86400

# Server Keys (loaded from environment variables)
# Keys are NOT stored by the server. Developers manage their own key lifecycle.
#
# IMPORTANT: Generate keys using the provided script:
#   deno run --allow-write scripts/generate-keys.ts
#
# This will create .env.keys with your keys. Copy them below, then DELETE .env.keys for security.
#
# Two key pairs are required:
#   - Identity keys (Ed25519) - For signing/authentication
#   - Encryption keys (X25519) - For encryption
#
# Public keys MUST be 64 hex characters (32 bytes raw), NOT DER format
#
# SERVER_IDENTITY_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# SERVER_IDENTITY_PUBLIC_KEY_HEX="your-ed25519-public-key-in-hex-format-64-chars"
# SERVER_ENCRYPTION_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# SERVER_ENCRYPTION_PUBLIC_KEY_HEX="your-x25519-public-key-in-hex-format-64-chars"

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://app.example.com

# Password reset token TTL (in seconds, default 3600 = 1 hour)
PASSWORD_RESET_TOKEN_TTL_SECONDS=3600
