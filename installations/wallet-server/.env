# B3nd Wallet Server Configuration

# Server port
WALLET_PORT=3001

# B3nd backend URLs
# Credential backend - stores user keys, passwords, and reset tokens
CREDENTIAL_NODE_URL=http://localhost:8080

# Proxy backend - where user writes are forwarded to
# Can be the same as CREDENTIAL_NODE_URL in development
PROXY_NODE_URL=http://localhost:8080

# JWT Configuration
# Generate with: head -c 32 /dev/urandom | base64
JWT_SECRET=uI6WfYXpUTqep8TVDt/SGlsibtmCAZp9aTvi376DZGY=

# JWT token expiration (in seconds, default 86400 = 24 hours)
JWT_EXPIRATION_SECONDS=86400

# Server Keys (loaded from environment variables)
# Keys are NOT stored by the server. Developers manage their own key lifecycle.
#
# Generate Ed25519 identity key:
#   openssl genpkey -algorithm ed25519 -outform PEM -out identity.pem
#   openssl pkey -in identity.pem -pubout -outform DER | xxd -p -c 256
#
# Generate X25519 encryption key:
#   openssl genpkey -algorithm x25519 -outform PEM -out encryption.pem
#   openssl pkey -in encryption.pem -pubout -outform DER | xxd -p -c 256
#
# To extract just the key material (without headers):
#   openssl pkey -in identity.pem -text -noout
#
SERVER_IDENTITY_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\nMC4CAQAwBQYDK2VwBCIEIK5NgYvmOA6QmEzRzRlv+5bR5tZ9nebYj42VVRMCts6O\n-----END PRIVATE KEY-----"
SERVER_IDENTITY_PUBLIC_KEY_HEX="2f79e4d93e8471f77628b5b47acfd194dcdadbfd73f67ac564276c5048682f3f"
SERVER_ENCRYPTION_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\nMC4CAQAwBQYDK2VuBCIEIAaM/ncPUZhN1bVkQ9TVuvlfs+DR7CbswhkKvk1o03Bh\n-----END PRIVATE KEY-----"
SERVER_ENCRYPTION_PUBLIC_KEY_HEX="1e0e2e2f87c287f0f4fbcef09674892f49a632a9befbb9615b9004b77c7a9a0e"

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://app.example.com

# Password reset token TTL (in seconds, default 3600 = 1 hour)
PASSWORD_RESET_TOKEN_TTL_SECONDS=3600
