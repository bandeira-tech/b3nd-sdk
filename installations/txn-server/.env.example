# B3nd Transaction Server Configuration

# Server port (required)
PORT=8843

# CORS allowed origin (required)
# Use "*" for development, specific origin for production
CORS_ORIGIN=*

# Path to validator module (required)
# The module must export a default TransactionValidator function
VALIDATOR_MODULE=./example-validator.ts

# Read backend URL(s) for state validation (required)
# Comma-separated for fallback chain
# Supports: memory://, http://, https://
READ_BACKEND_URL=http://localhost:8842

# Peer URLs for transaction propagation (optional)
# Comma-separated list of peer transaction nodes or data nodes
# Supports: http://, https://, memory://
PEER_URLS=

# Whether to wait for peer propagation before returning (optional)
# Default: false (async propagation)
AWAIT_PROPAGATION=false
