/**
 * Console backend for b3nd.
 *
 * Write-only Store that logs operations to the console. Useful for debugging,
 * auditing, and piping protocol traffic to the terminal.
 */

export { ConsoleStore } from "./store.ts";
