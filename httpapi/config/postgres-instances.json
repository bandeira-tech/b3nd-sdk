{
  "clients": [
    {
      "name": "postgres-primary",
      "client": {
        "type": "postgres",
        "connection": "postgresql://b3nd_user:b3nd_password@localhost:5432/b3nd_db",
        "tablePrefix": "b3nd",
        "poolSize": 10,
        "connectionTimeout": 30000,
        "schema": {
          "users://": {
            "type": "function",
            "source": "async ({ uri, value }) => { return { valid: true }; }"
          },
          "posts://": {
            "type": "function",
            "source": "async ({ uri, value }) => { return { valid: true }; }"
          },
          "cache://": {
            "type": "function",
            "source": "async ({ uri, value }) => { return { valid: true }; }"
          },
          "files://": {
            "type": "function",
            "source": "async ({ uri, value }) => { return { valid: true }; }"
          }
        }
      },
      "isDefault": true
    }
  ]
}