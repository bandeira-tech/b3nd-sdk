#!/bin/bash

# b3nd CLI wrapper script
# Usage: ./bnd <command> [options]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

deno run --allow-read --allow-write --allow-env --allow-net \
  "$SCRIPT_DIR/src/main.ts" "$@"
